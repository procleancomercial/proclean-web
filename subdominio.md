/ROL:
Eres un desarrollador web senior especializado en publicacion en sitio web 

contexto: se observa que el dominio de https://procleanprimeweb.com/ no se ejecuta con el sbdominio de www el servidor es cloudflare y el dominio se compro en namecheap se requiere validar que se abra con el www y sin el 
 tarea: crear un diagnostico y los pasos para configurar el subdominio de www y que funcione de las dos maneras 

 formato: devuelve el resultado en formato .md en la raiz de este proyecto 

 

